%h1 Listing all cities here
%table
  %thead
    %tr
      %th Nombre
      %th Alcalde
      %th Edit
      %th Elimnar
  %tbody
    - if @cities.blank?
      %tr
        %td{:colspan => "6"}
          %em No se han encontrado ciudades
    - @cities.each do |city|
      %tr
        %td= city.name
        %td= User.all[city.mayor_id].name
        %td= link_to 'editar', edit_city_url(city)
        %td
          = link_to [city], method: :delete do
            Eliminar

<div class="form-row">
      <div class="form-title">
            Edición de <%=@faction.name%>
      </div>
    <%= form_for [@faction], html: {class: 'form-horizontal' } do |f| %>
      <%= render 'form_edit', f: f %>
      <br/>
      <div class="field">
          <%= f.submit 'Guardar', class:'btn btn-primary' %>
      </div>
    <% end %>
</div>

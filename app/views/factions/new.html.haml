.row
  %h2 Creando un nuevo proyecto
  = form_for [@faction], html: {class: 'form-horizontal' } do |f|
    = render 'form', f: f

    %br

    .field
      = f.submit 'Guardar', class:'btn btn-primary pull-right'

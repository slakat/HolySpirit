.form-row
      .form-title
            Crea tu facción
      .row
            = form_for [@faction], html: {class: 'form-horizontal' } do |f|
                  = render 'form', f: f

                  %br

                  .field
                        = f.submit 'Guardar', class:'btn btn-primary'

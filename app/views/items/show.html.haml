%h1 Mostrando un item
%table
  %thead
    %tr
      %th Nombre
      %th Descripción
      %th Effectos
      %th Dueño
      %th Edit
      %th Elimnar
  %tbody
    - if @item.blank?
      %tr
        %td{:colspan => "6"}
          %em No se ha encontrado el item
    - else
      %tr
        %td= @item.name
        %td= @item.description
        / Declarado en el modelo
        %td= @item.effect
        %td= @item.user.name
        %td= link_to 'editar', edit_item_url(@item)
        %td
          = link_to [@item], method: :delete do
            Eliminar

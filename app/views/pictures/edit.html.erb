<div class="form-row">
      <div class="form-title">
            Edición una foto de <%=@picture.point.name%>
      </div>
      <div class="row">

          <%= form_for [@picture], html: {class: 'form-horizontal' } do |f| %>
            <%= render 'form_edit', f: f %>

            <br/>
            <div class="field">
                <%= f.submit 'Guardar', class:'btn btn-primary' %>
          <% end %>
        </div>
      </div>
</div>

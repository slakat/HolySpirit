
<div class="form-row">
      <div class="form-title">
            Estás editando <%=@point.name%>
      </div>

<div class="row">
    <%= form_for [@point], html: {class: 'form-horizontal' } do |f| %>
      <%= render 'form', f: f %>
      <br/>
      <div class="field">
        <div class="col-md-4 col-md-offset-8">
          <%= f.submit 'Guardar', class:'btn btn-primary' %>
        </div>
      </div>
    <% end %>
  </div>
</div>

.row
  %h2 Creando un nuevo usuario
  %h5 Tu username debe tener entre 4 y 30 caracteres.
  %h5 La clave no es case sensitive
  = form_for [@user], html: {class: 'form-horizontal' } do |f|
    = render 'form', f: f

    %br

    .field
      = f.submit 'Guardar', class:'btn btn-primary pull-right'
